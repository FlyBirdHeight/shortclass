spring:
    datasource:
        driver-class-name: com.mysql.jdbc.Driver
        username: root
        password:
        url: jdbc:mysql://127.0.0.1:3306/short?characterEncoding=utf8
        ip: localhost
        port: 3306
        database: android

    thymeleaf:
        cache: false
        check-template: false
        mode: LEGACYHTML5

mybatis:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: com.adsion.bean

server:
    port: 80